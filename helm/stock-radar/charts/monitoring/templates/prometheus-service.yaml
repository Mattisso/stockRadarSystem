apiVersion: v1
kind: Service
metadata:
  name: stock-radar-prometheus
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: stock-radar-prometheus
  ports:
    - port: 9090
      targetPort: 9090
