apiVersion: v1
kind: Service
metadata:
  name: stock-radar-grafana
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: stock-radar-grafana
  ports:
    - port: 3000
      targetPort: 3000
